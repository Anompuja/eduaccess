import 'package:flutter_test/flutter_test.dart';

// Widget tests for EduAccess will be added per feature module.
// The app uses Riverpod ProviderScope + GoRouter which require
// proper test setup — see features/*/test/ for feature-level tests.
void main() {
  test('placeholder', () => expect(true, isTrue));
}
